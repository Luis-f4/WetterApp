wo ist der Fehler im react code:


    const handleEditClick = (row, idLIcensing) => {

        console.log("jetzt wird die funktion ausgeführt");
        console.log("idLIcensing: ", idLIcensing);

        setSelectedRow(row); 
        setSelectedIdLicensing(idLIcensing);

 
        console.log("selectedIdLicensing: ", selectedIdLicensing);
    };

    const closePopup = () => {

        console.log("selectedIdLicensing VORHER: ", selectedIdLicensing);

        setSelectedRow(null); // Popup schließen
        setSelectedIdLicensing(null);

        console.log("selectedIdLicensing NACHHER: ", selectedIdLicensing);
    };


ergebniss in der console:


jetzt wird die funktion ausgeführt
TableEmployee.js:28 idLIcensing:  5
TableEmployee.js:34 selectedIdLicensing:  null
TableEmployee.js:39 selectedIdLicensing VORHER:  5
TableEmployee.js:44 selectedIdLicensing NACHHER:  5
