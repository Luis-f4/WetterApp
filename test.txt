{mode !== 'License' && (
    <select name="cars" id="cars">
        <option value="audi">testPack1 | PO: 1231 | amount: 19</option>
        {dropdownOptions.map((option, index) => (
            <option key={index} value={option[0]}>
                {option[0]} | PO: {option[1]} | amount: {option[2]}
            </option>
        ))}
    </select>
)}
