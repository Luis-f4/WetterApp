<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filter Lists</title>
</head>
<body>
    <textarea id="tt" placeholder="DB"></textarea>
    <textarea id="tt2" placeholder="Liste"></textarea>
    <button onclick="filterList()">Gen</button>
    <textarea id="Ausgabe"></textarea>

    <script>
        function filterList() {
            var list1 = document.getElementById("tt").value.split("\n");  // Hier wird Liste 1 abgerufen
            var list2 = document.getElementById("tt2").value.split("\n");  // Hier wird Liste 2 abgerufen

            var result = [];
            for (var i = 0; i < list1.length; i++) {
                if (list2.indexOf(list1[i].trim()) === -1) {
                    result.push(list1[i]);
                }
            }

            //if(BTS){

                for (var x = 0; x < result.length; x++) {
                    result[x] = `insert Into test_label(Spalte1, Spalte2) values("value1","` + result[x] + `");`;
                }

            //}else{
                for (var x = 0; x < result.length; x++) {
                    result[x] = `insert Into test_label(Spalte1, Spalte2) values("value1","` + result[x] + `");`;
                }
            //}


            document.getElementById("Ausgabe").value = result.join("\n");
        }
    </script>
</body>
</html>


