korrigiere:


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Square Advanture</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/1.4.0/p5.js"></script>
    <script src="script.js"></script>
</head>
<body>
    <h1 id="h1" style="position: absolute; left: 35%; top: 30%; font-size:700%; color: gold; opacity: 0;">GEWONNEN!</h1>
    <button id="btn" onclick="standartZeichnung()" style="display: none; position: absolute; left: 45%; top: 45%;">Neustart</button>
</body>
</html>






let player; // Spielerposition
let jumpForce = 0;
let movement = 0;
let ground;
let platforms = [];
let coin;

function setup() {
    createCanvas(1900, 900);
    player = createVector(width / 2, height - 50);
    platforms.push(createVector(100, 800));
    platforms.push(createVector(400, 650));
    platforms.push(createVector(650, 500));
    platforms.push(createVector(1000, 350));
    coin = createVector(1200, 200); 

    ground = height - 50;
}





function standartZeichnung(){
    background('DeepSkyBlue');
    rectMode(CENTER);
    
    // Spieler zeichnen
    fill('white');
    rect(player.x, player.y, 50, 50);

    // Plattformen zeichnen
    fill('green');
    for (let platform of platforms) {
        rect(platform.x, platform.y, 150, 20);
    }

    // Coin zeichnen
    if (coin) {
        fill('gold');
        ellipse(coin.x, coin.y, 30, 30); // Coin rund machen
    }

}
function draw() {

    standartZeichnung();
    // Bewegung und Schwerkraft
    player.y += jumpForce;
    player.x += movement;

    let onGround = false;

    // Überprüfung: Boden oder Plattformen
    if (player.y >= ground) {
        onGround = true;
        player.y = ground;
    } else {
        for (let platform of platforms) {
            if (
                player.x + 25 >= platform.x - 75 &&
                player.x - 25 <= platform.x + 75 &&
                player.y + 25 >= platform.y - 10 &&
                player.y + 25 <= platform.y + 10
            ) {
                onGround = true;
                player.y = platform.y - 35;
                break;
            }
        }
    }

    // Schwerkraft anwenden, wenn nicht auf dem Boden oder Plattform
    if (!onGround) {
        jumpForce += 0.5;
    } else {
        jumpForce = 0;
    }

    // Springen
    if (keyIsDown(32) && onGround) {
        jumpForce = -12;
    }

    // Links/Rechts Bewegung
    if (keyIsDown(LEFT_ARROW)) {
        movement = -10;
    } else if (keyIsDown(RIGHT_ARROW)) {
        movement = 10;
    } else {
        movement = 0;
    }

    
    if (coin && dist(player.x, player.y, coin.x, coin.y) < 40) {
        coin = null; 
        ground = null;
        platforms = null;
        player = null;
        document.getElementById("h1").style.opacity = 1;
        document.getElementById("btn").style.display = "block";

        

        
       
    }
}
