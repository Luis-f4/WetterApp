wo ist der Fehler:


    const handleEditClick = (row, idLIcensing) => {
        console.log("jetzt wird die funktion ausgeführt");
        console.log("idLIcensing: ", idLIcensing);

        setSelectedRow(row); 
        setSelectedIdLicensing(idLIcensing);

        console.log("idLIcensing als const: ", selectedIdLicensing);
    };

    const closePopup = () => {
        console.log("jetzt wird die funktion geschlossen");
        console.log("idLIcensing als const VORHER: ", selectedIdLicensing);

        setSelectedRow(null); // Popup schließen
        setSelectedIdLicensing(null);

        console.log("idLIcensing als const NACHHER: ", selectedIdLicensing);
    };



console.log:

jetzt wird die funktion ausgeführt
TableEmployee.js:27 idLIcensing:  5
TableEmployee.js:32 idLIcensing als const:  null
TableEmployee.js:36 jetzt wird die funktion geschlossen
TableEmployee.js:37 idLIcensing als const VORHER:  5
TableEmployee.js:42 idLIcensing als const NACHHER:  5
